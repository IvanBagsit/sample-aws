spring:
  application:
    name: sample-cloud
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  datasource:
    username: ${DB_USER:admin}
    password: ${DB_PASS:admin}
    url: jdbc:mysql://${DB_HOST:localhost}/${DB_NAME:sample_cloud}
    driverClassName: com.mysql.cj.jdbc.Driver